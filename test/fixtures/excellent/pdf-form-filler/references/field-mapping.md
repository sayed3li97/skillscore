# Field mapping reference

Radio groups expose one field with several export values; set the field to
the export value of the option to select.

Nested names use dot notation: `applicant.address.zip`.

Checkbox fields accept `"Yes"` to tick and `"Off"` to clear.
